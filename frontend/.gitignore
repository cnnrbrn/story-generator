# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (keep .env.example tracked)
.env
.env.*
!.env.example

# Note: src/routeTree.gen.ts is committed (TanStack starter convention) so that
# `tsc -b` in the build script finds it before Vite regenerates it.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
